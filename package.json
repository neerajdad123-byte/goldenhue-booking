{
  "name": "goldenhue-test-harness",
  "version": "1.0.0",
  "private": true,
  "description": "Tooling for the checks in work/. The application lives in outputs/demo and has its own package.json, which is what gets deployed.",
  "type": "commonjs",
  "scripts": {
    "test": "powershell -NoProfile -ExecutionPolicy Bypass -File work/suite.ps1"
  },
  "dependencies": {
    "@electric-sql/pglite": "^0.5.8",
    "@electric-sql/pglite-socket": "^0.2.11",
    "pg": "^8.23.0"
  }
}
